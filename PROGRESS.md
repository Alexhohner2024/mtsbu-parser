# MTSBU Parser — Что сделано

## Дата: 24.05.2026

---

## Обновление: замена Playwright на CloakBrowser

**Проблема:** Playwright не проходил Turnstile автоматически — требовалось каждый раз кликать капчу вручную.

**Решение:** Заменён `playwright` на `cloakbrowser` — патченный Chromium с 58 C++ патчами, который выглядит как реальный браузер.

### Что изменилось:
- Turnstile теперь проходится **полностью автоматически**
- `--headless` работает (раньше нельзя было — капчу нужно было кликать)
- `humanize=True` — движения мыши, клики и ввод как у человека
- Фиксированный fingerprint (`--fingerprint=12345`) — выглядит как один пользователь

### Результаты тестов:
- ❌ `BH7246MP` → полис не найден (корректно)
- ✅ `ВН1654ОА` → полис найден: №238804679, діючий, АТ "СГ "ТАС", CHERY TIGGO

### Коммит: `2a4f59d`

---

## Дата: 16.05.2026

---

## Проблема
Нужно проверять страховку ОСЦПВ по госномеру через `policy.mtsbu.ua`.
Сайт защищён Cloudflare Turnstile (капча), которая блокирует автоматические запросы.

## Решение (устаревшее)
Создан локальный скрипт `check_policy.py` на базе Playwright.

### Как работает:
1. Открывает реальный браузер (Chromium)
2. Переходит на `policy.mtsbu.ua`
3. Заполняет госномер и дату
4. Ждёт решения Turnstile (автоматически или вручную)
5. Отправляет форму
6. Парсит результат: статус полиса, страховая компания, данные ТС
7. Выводит в консоль + сохраняет JSON

### Файлы:
| Файл | Описание |
|------|----------|
| `check_policy.py` | Основной скрипт проверки |
| `requirements.txt` | Python зависимости |
| `lib/parseMtsbuHtml.js` | Парсер HTML (из старого проекта) |
| `api/parse.js` | Vercel serverless (из старого проекта) |

### Использование:
```bash
# Установка
pip3 install -r requirements.txt

# Проверка по госномеру
python3 check_policy.py AA1234BC

# С указанием даты
python3 check_policy.py AA1234BC --date 16.05.2026

# Сохранить результат в JSON
python3 check_policy.py AA1234BC --json result.json

# Фоновый режим (без окна браузера)
python3 check_policy.py AA1234BC --headless
```

### Вывод:
- **Консоль:** форматированный текст с эмодзи
- **JSON:** полный результат с метаданными

### Пример JSON:
```json
{
  "plate": "AA1234BC",
  "date": "16.05.2026",
  "checked_at": "2026-05-16T01:21:47.824549",
  "found": true,
  "policyNumber": "123456789",
  "status": "Чинний",
  "statusDate": "16.05.2026",
  "company": {
    "name": "СК «Название»",
    "status": "Діючий",
    "edrpou": "12345678",
    "address": "...",
    "phone": "...",
    "email": "..."
  },
  "vehicle": {
    "type": "Легковий",
    "make": "Toyota",
    "model": "Camry",
    "plate": "AA1234BC",
    "vin": "JTD..."
  }
}
```

---

## Текущие ограничения

- CloakBrowser требует скачивания бинарника (~200MB) при первом запуске
- Может не работать на macOS без предварительного подтверждения Gatekeeper (нужно 1 раз открыть через правый клик → Open)

---

## Что было исправлено в старом проекте

### constanta-water-temp
- Заменён CORS прокси: `corsproxy.io` → `api.codetabs.com`
- Обновлены regex-паттерны для новой структуры сайта
- Добавлен fallback на JSON-LD данные

---

## Зависимости
```
cloakbrowser>=0.3.30
beautifulsoup4>=4.12.0
lxml>=5.0.0
```
