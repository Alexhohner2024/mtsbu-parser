# MTSBU Parser — Что сделано

## Дата: 24.05.2026

---

## Обновление: замена Playwright на CloakBrowser

**Проблема:** Playwright не проходил Turnstile автоматически — требовалось каждый раз кликать капчу вручную.

**Решение:** Заменён `playwright` на `cloakbrowser` — патченный Chromium с 58 C++ патчами, который выглядит как реальный браузер.

### Что изменилось:
- Turnstile теперь проходится **полностью автоматически**
- `--headless` работает (раньше нельзя было — капчу нужно было кликать)
- `humanize=True` — движения мыши, клики и ввод как у человека
- Фиксированный fingerprint (`--fingerprint=12345`) — выглядит как один пользователь

### Результаты тестов:
- ❌ `BH7246MP` → полис не найден (корректно)
- ✅ `ВН1654ОА` → полис найден: №238804679, діючий, АТ "СГ "ТАС", CHERY TIGGO

### Коммит: `2a4f59d`

---

## Обновление 2: поиск даты окончания полиса

**Новый файл:** `find_policy_end.py`

Алгоритм:
1. Проверка сегодня → узнаём номер полиса
2. Экспоненциальный поиск **назад** (шаг 180→360 дней) — граница, где номер полиса меняется
3. Бинарный поиск (ordinal, без timedelta-бага) — точная дата начала
4. Конец = начало + 1 год - 1 день

### Рефакторинг:
- `MtsbuChecker` — класс с общим браузером (один CloakBrowser на все запросы)
- `check_with_retry` — 2 повтора при ошибках
- Бинарный поиск через `toordinal()` — фикс бага `timedelta // 2` (добавлял часы, `days` округлялся)

### Вывод:
- 🆔 Поліс, 🏢 СК, 🚗 Авто, 📂 Тип, 🔢 Госномер, 🔍 VIN
- 📅 Початок / ⏳ Закінчення / ⏰ Залишилось

### Исправления парсера:
- "Марка та модель" — единое поле на сайте (для грузовиков C-категории)

### Тесты:
| Госномер | Поліс | Початок | Закінчення | Осталось |
|----------|-------|---------|------------|----------|
| ВН1654ОА | №238804679 | 21.05.2026 | 20.05.2027 | 361 дн |
| CE2733EM | №229648022 | 30.05.2025 | 29.05.2026 | 5 дн |
| ВН9208ІА | №229665873 | 01.06.2025 | 31.05.2026 | 7 дн |
| НН7998АК | №238356079 | 05.05.2026 | 04.05.2027 | 345 дн |

### Коммиты:
- `41747f4` — feat: find_policy_end.py
- `a4eed24` — refactor: MtsbuChecker + ordinal fix
- `43c3c9f` — fix: step 90→180
- `0726e29` — feat: full details with emojis
- `a1f37eb` — fix: parse "Марка та модель"

---

## Дата: 16.05.2026

---

## Проблема
Нужно проверять страховку ОСЦПВ по госномеру через `policy.mtsbu.ua`.
Сайт защищён Cloudflare Turnstile (капча), которая блокирует автоматические запросы.

## Решение (устаревшее)
Создан локальный скрипт `check_policy.py` на базе Playwright.

### Как работает:
1. Открывает реальный браузер (Chromium)
2. Переходит на `policy.mtsbu.ua`
3. Заполняет госномер и дату
4. Ждёт решения Turnstile (автоматически или вручную)
5. Отправляет форму
6. Парсит результат: статус полиса, страховая компания, данные ТС
7. Выводит в консоль + сохраняет JSON

### Файлы:
| Файл | Описание |
|------|----------|
| `check_policy.py` | Основной скрипт проверки |
| `requirements.txt` | Python зависимости |
| `lib/parseMtsbuHtml.js` | Парсер HTML (из старого проекта) |
| `api/parse.js` | Vercel serverless (из старого проекта) |

### Использование:
```bash
# Установка
pip3 install -r requirements.txt

# Проверка по госномеру
python3 check_policy.py AA1234BC

# С указанием даты
python3 check_policy.py AA1234BC --date 16.05.2026

# Сохранить результат в JSON
python3 check_policy.py AA1234BC --json result.json

# Фоновый режим (без окна браузера)
python3 check_policy.py AA1234BC --headless
```

### Вывод:
- **Консоль:** форматированный текст с эмодзи
- **JSON:** полный результат с метаданными

### Пример JSON:
```json
{
  "plate": "AA1234BC",
  "date": "16.05.2026",
  "checked_at": "2026-05-16T01:21:47.824549",
  "found": true,
  "policyNumber": "123456789",
  "status": "Чинний",
  "statusDate": "16.05.2026",
  "company": {
    "name": "СК «Название»",
    "status": "Діючий",
    "edrpou": "12345678",
    "address": "...",
    "phone": "...",
    "email": "..."
  },
  "vehicle": {
    "type": "Легковий",
    "make": "Toyota",
    "model": "Camry",
    "plate": "AA1234BC",
    "vin": "JTD..."
  }
}
```

---

## Текущие ограничения

- CloakBrowser требует скачивания бинарника (~200MB) при первом запуске
- Может не работать на macOS без предварительного подтверждения Gatekeeper (нужно 1 раз открыть через правый клик → Open)

---

## Что было исправлено в старом проекте

### constanta-water-temp
- Заменён CORS прокси: `corsproxy.io` → `api.codetabs.com`
- Обновлены regex-паттерны для новой структуры сайта
- Добавлен fallback на JSON-LD данные

---

## Зависимости
```
cloakbrowser>=0.3.30
beautifulsoup4>=4.12.0
lxml>=5.0.0
python-dateutil>=2.8.0
customtkinter>=5.2.0
Pillow>=10.0.0
pyperclip>=1.8.2
```

---

## Дата: 24.05.2026 — Windows GUI (v2.0)

Рефакторинг и создание настольного приложения на CustomTkinter.

### Архитектура

```
mtsbu-parser/
├── app.py                   ← Точка входа GUI
├── core/
│   ├── parser.py            ← Парсинг HTML (выделен из check_policy.py)
│   ├── checker.py           ← MtsbuChecker + поиск по госномеру/VIN
│   └── finder.py            ← Алгоритм поиска даты окончания
├── gui/
│   ├── app_window.py        ← Главное окно + worker thread + Mica-эффект
│   ├── input_frame.py       ← Радио (госномер/VIN) + поле + ✂️ Вставка
│   ├── log_panel.py         ← Лог процесса с автоскроллом
│   └── result_frame.py      ← Карточка результата + 📋 Копировать
├── check_policy.py          ← CLI (обновлён, импорт из core/)
├── find_policy_end.py       ← CLI (обновлён, импорт из core/)
├── build.spec               ← PyInstaller spec
└── dist/MTSBU_Parser.exe    ← Готовый .exe (58MB, без консоли)
```

### Что сделано

#### Рефакторинг
- `check_policy.py` разбит на `core/parser.py`, `core/checker.py`, `core/finder.py`
- `find_policy_end.py` переписан на использование модулей и колбэков статуса
- CLI-скрипты `check_policy.py` и `find_policy_end.py` сохранены (тонкие обёртки над core/)

#### VIN-поиск
- Добавлен метод `check_by_vin()` — вкладка `#vin` на policy.mtsbu.ua
- Универсальный `_check()` для plate и VIN (без дублирования кода)

#### Ссылка на полис
- Захватывается `page.url` после отправки формы → сохраняется как `result["url"]`
- Отображается в результатах и при копировании

#### Ожидание результата (`_submit_and_wait`)
- Вместо `wait_for_load_state` — цикл из 30 попыток по 1с
- Проверяет смену URL (наличие `Search/...`) или появление слова "поліс" в HTML
- Если страница не изменилась — читает текущий HTML

#### Дебаг HTML
- Если парсинг не нашёл полис — HTML сохраняется в `%TEMP%/mtsbu_debug_*.html` (первые 10k символов)

#### Парсинг (улучшен)
- Fallback на `main`/`article`/`body`, если нет `div.content`
- Regex-поиск номера полиса по всему тексту, если не нашли в title_block
- Возвращает `not found` только если совсем нет номера

#### GUI (CustomTkinter)
- **Окно:** 620x620, тёмная тема + синий акцент
- **Стекло:** `alpha=0.97` + Mica-эффект (Windows 11) через `DwmSetWindowAttribute`
- **Ввод:** радио «За госномером / За VIN-кодом», поле ввода, кнопка 📋 вставки
- **Вставка:** `pyperclip.paste()` вместо `tkinter.clipboard_get()` (работает в frozen .exe)
- **Ctrl+V / правый клик → Вставить**
- **Лог:** процесс с иконками, автоскролл через `update_idletasks()`
- **Результат:** карточка со всеми полями, ссылка на policy.mtsbu.ua
- **Копирование:** кнопка «📋 Копіювати все» → форматированный текст в буфер
- **Stale-защита:** последовательный номер поиска, старые результаты игнорируются
- **Worker:** background thread + `after(0, ...)` для GUI-обновлений

#### Сборка PyInstaller
- `--onefile --windowed` — один .exe без консоли
- `build.spec` с hiddenimports для `tkinter`, `customtkinter`, `playwright`, `cloakbrowser`, etc.
- Размер: ~58MB (включает Python + Playwright + CloakBrowser + зависимости)

### Ограничения
- Первый запуск CloakBrowser скачивает бинарник ~200MB
- VIN-таб использует предполагаемые селекторы (`#vin`, `#VinModel_VinCode`) — уточнить при необходимости
- Mica-эффект только на Windows 11 (на Win10 просто тёмная тема)
